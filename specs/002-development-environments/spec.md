# Development environments

Provide locked development tools and a source-free image for Image Utils. Run the existing make check test-all contract with Python, GNU tools, ShellCheck, and ImageMagick 7. Preserve generated catalog ownership, source retention, literal filenames, no-clobber publication, and read-only MCP boundaries.

Docker, Podman, and Apple adapters preserve arguments and caller ownership. Validate actual image operations using disposable fixtures, record optional delegate skips, and leave product runtime images and publication contracts intact.
